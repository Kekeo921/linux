{% for host in ansible_play_hosts_all %}
tls_server_config:
  cert_file: /etc/alertmanager/keys/{{hostvars[host]['ansible_facts']['hostname']}}.crt
  key_file: /etc/alertmanager/keys/{{hostvars[host]['ansible_facts']['hostname']}}.key
{% endfor %}
